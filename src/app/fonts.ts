import { DM_Sans, Mulish, Rubik } from "next/font/google";

export const dm_sans = DM_Sans({ subsets: ["latin"] });
export const rubik = Rubik({ subsets: ["latin"] });
export const mulish = Mulish({ subsets: ["latin"] });
